# Dependencies
node_modules/

# Build output
dist/
.cache/

# Environment files — never commit real secrets
.env
.env.local
.env.*.local
# .env.example is intentionally NOT ignored — it's the reference template

# Logs
*.log
logs/

# Test artifacts
coverage/
.vitest/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# TypeScript build info
*.tsbuildinfo

# Prisma generated — regenerated by `pnpm prisma:generate`
# (committed intentionally so CI doesn't need a DB to typecheck)
# src/generated/
